{
  "name": "nextfy-deploy",
  "version": "1.0.0",
  "description": "Proxy package para deploy automático via Git",
  "scripts": {
    "start": "cd backend && npm install && node server.js",
    "build": "cd backend && npm install"
  }
}
